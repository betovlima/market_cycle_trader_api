from __future__ import annotations

TARGET = "source_direct_delta_log_capital"
EXPERIMENT_NAME = "contextual_marginal_signature_direct_marginal_tournament"


def main(*, script_version: str) -> int:
    raise RuntimeError("Tournament implementation bootstrap pending.")
