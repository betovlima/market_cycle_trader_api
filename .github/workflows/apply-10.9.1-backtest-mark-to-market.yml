name: API v10.9.1 patch workflow disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "One-shot patch already applied; workflow intentionally disabled."
