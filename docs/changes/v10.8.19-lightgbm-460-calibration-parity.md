# API v10.8.19 — LightGBM 4.6.0 calibration parity

## Context

A real API 10.8.18 Simulation successfully validated all of the following:

- fresh full Alpaca RAW refresh;
- RAW research protocol;
- 55 eligible assets from 56 configured assets;
- DOC structural exclusion to PEAK;
- 17 causal split events;
- no dividend back-adjustment;
- deterministic execution;
- numeric thread limit 1;
- LightGBM n_jobs 1.

Despite that, the Control result remained:

```text
US$ 5,544,605.11
```

and fold 1 still selected:

```text
calibrated_candidate_margin = 0.0100
```

The homologated TCC direct-Alpaca experiment that produced:

```text
Control = US$ 10,082,425.91
Soft    = US$ 9,840,028.41
Soft - Control = -2.40%
Soft changed base actions = 5
```

used the same NumPy, pandas, SciPy and scikit-learn versions but a different
LightGBM runtime.

## Runtime difference

TCC direct-Alpaca reference:

```text
lightgbm = 4.6.0
numpy = 2.5.1
pandas = 3.0.5
scipy = 1.18.0
scikit-learn = 1.9.0
```

MCT API 10.8.18 run:

```text
lightgbm = 4.7.0
numpy = 2.5.1
pandas = 3.0.5
scipy = 1.18.0
scikit-learn = 1.9.0
```

The MCT dependency previously allowed any LightGBM release in:

```text
>=4.6,<5
```

which installed 4.7.0 in the current local environment.

## Change

API 10.8.19 pins:

```text
lightgbm==4.6.0
```

No strategy parameter, fold boundary, market-data rule, fee, target horizon or
switch-margin candidate is changed by this version.

The research-effective execution remains deterministic:

```text
deterministic_execution = true
numeric_thread_limit = 1
LightGBM n_jobs = 1
early_stopping_enabled = false
```

## Calibration diagnostics

Each fold now exports the score of every configured switch-margin candidate:

```text
calibration_candidate_scores = [
  {"margin": 0.0000, "risk_adjusted_score": ...},
  {"margin": 0.0025, "risk_adjusted_score": ...},
  {"margin": 0.0050, "risk_adjusted_score": ...},
  {"margin": 0.0100, "risk_adjusted_score": ...}
]
```

and:

```text
calibration_score_gap_best_vs_second
```

This removes ambiguity around why a discrete margin was selected.

## Parity checkpoint

The first decisive checkpoint remains fold 1.

Expected homologated behavior:

```text
fold 1 calibrated_candidate_margin = 0.0000
fold 2 calibrated_candidate_margin = 0.0100
fold 3 calibrated_candidate_margin = 0.0000
```

If fold 1 returns to 0.0000, the OOS path should again allow the early
2020-07-28 CXW → MGM rotation that is present in the ~US$ 10.08M TCC run.

## Branch

`fix/v10.8.19-lightgbm-460-parity`

## Validation procedure

1. install requirements so the environment is actually downgraded to
   LightGBM 4.6.0;
2. restart the API process;
3. run one Backtest Simulation from the front;
4. export the result ZIP;
5. confirm runtime `lightgbm = 4.6.0`;
6. inspect all fold calibration candidate scores before changing anything else;
7. do not start Model Tuning until the Control parity question is resolved.
