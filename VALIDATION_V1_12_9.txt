Market Cycle Trader API v1.12.9 validation

Validated changes:
- API version updated to 1.12.9 in pyproject.toml and core/config.py.
- scikit-learn added to requirements.txt.
- Empty market series return a UTC DatetimeIndex.
- Exit diagnostics no longer compare a RangeIndex with a pandas Timestamp.
- Missing future bars are skipped without failing the complete results endpoint.
- Market timestamps are normalized to UTC.
- Duplicate timestamps are deduplicated.

Focused regression tests:
- Empty typed market series.
- Default empty pandas Series with RangeIndex.
- Naive DatetimeIndex normalization to UTC.
- Invalid non-datetime index handling.
- Full performance-diagnostics call with a SELL and no future MongoDB bars.

Result: 5 passed.

Additional validation:
- All Python source files parsed successfully.
- pyproject.toml and railway.toml parsed successfully.
- No __pycache__, .pyc, .pytest_cache, .env, or virtual-environment files included in the archive.

Environment note:
- The focused tests used a minimal temporary pymongo import stub because pymongo was not installed in the artifact-building environment. The delivered requirements.txt includes pymongo for Railway and local installation.
